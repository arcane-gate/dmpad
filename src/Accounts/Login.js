import React from "react";
import { login } from "./auth";

const Login = () => {
  return (
    <div>
      <input type="email" />
      <input type="password" />
    </div>
  );
};

export default Login;
