import React from "react";
import { signup } from "./auth";

const Signup = () => {
  return (
    <div>
      <input type="email" />
      <input type="password" />
      <input type="password" />
    </div>
  );
};

export default Signup;
